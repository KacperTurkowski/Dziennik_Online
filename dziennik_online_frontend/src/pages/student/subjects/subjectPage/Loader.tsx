import React from "react";
import Loading from '../../../../components/loading/Loading';

export const Loader = () => {
    return (
        <div className={'loading'}>
            <Loading/>
        </div>
    )
}